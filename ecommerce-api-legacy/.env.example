# Environment variables read by src/config. Export them in the environment (the app does not load a .env file itself). All keys are optional.
# HTTP port; defaults to 3000.
PORT=3000
# Password for the seeded demo account. When unset, the seed gets a random, unknown password.
SEED_USER_PASSWORD=<choose-a-password>
