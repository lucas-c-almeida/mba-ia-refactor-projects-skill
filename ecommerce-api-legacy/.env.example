# Copy to .env (or export in the shell) — every key is optional.
# Port the HTTP server listens on (default: 3000)
PORT=3000
# SQLite database location (default: :memory: — re-created and re-seeded on every boot)
DB_PATH=:memory:
